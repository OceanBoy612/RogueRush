extends Control

#
#onready var projectResolution = get_viewport().size
#
#
#
#func _process(delta):
#	#doesn't work to remove the movement of the ui
#	rect_global_position = -get_parent().offset + get_parent().global_position - (projectResolution/2)
